appWatcher:
  module: appWatcher
  class: AppWatcher
  notify_name: kevin
  notify_message: "AppDaemon error: {}"
  #notify_message: "Appdaemon reported an error: {}"
  dependencies:
    - Notifier
  global_dependencies:
    - globals
    - secrets